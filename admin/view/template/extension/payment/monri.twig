{{ header }}{{ column_left }}
<div id="content">
    <div class="page-header">
        <div class="container-fluid">
            <div class="pull-right">
                <button type="submit" form="form-payment" data-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fa fa-save"></i></button>
                <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-default"><i class="fa fa-reply"></i></a></div>
            <h1>{{ heading_title }}</h1>
            <ul class="breadcrumb">
                {% for breadcrumb in breadcrumbs %}
                    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <div class="container-fluid">
        {% if error_warning %}
            <div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> {{ error_warning }}
                <button type="button" class="close" data-dismiss="alert">&times;</button>
            </div>
        {% endif %}
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title"><i class="fa fa-pencil"></i> {{ text_edit }}</h3>
            </div>
            <div class="panel-body">
                <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-payment" class="form-horizontal">

                    <!-- Input test mode -->
                    <div class="form-group">
                        <label class="col-sm-2 control-label">{{ entry_test }}</label>
                        <div class="col-sm-10">
                            <label class="radio-inline">
                                {% if payment_pikpay_test %}
                                    <input type="radio" name="payment_pikpay_test" value="1" checked="checked" />
                                    {{ text_yes }}
                                {% else %}
                                    <input type="radio" name="payment_pikpay_test" value="1" />
                                    {{ text_yes }}
                                {% endif %}
                            </label>
                            <label class="radio-inline">
                                {% if not payment_pikpay_test %}
                                    <input type="radio" name="payment_pikpay_test" value="0" checked="checked" />
                                    {{ text_no }}
                                {% else %}
                                    <input type="radio" name="payment_pikpay_test" value="0" />
                                    {{ text_no }}
                                {% endif %}
                            </label>
                        </div>
                    </div>

                    <!-- Input Enable/Disable module -->
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-status">{{ entry_status }}</label>
                        <div class="col-sm-10">
                            <select name="payment_pikpay_status" id="input-status" class="form-control">
                                {% if payment_pikpay_status %}
                                    <option value="1" selected="selected">{{ text_enabled }}</option>
                                    <option value="0">{{ text_disabled }}</option>
                                {% else %}
                                    <option value="1">{{ text_enabled }}</option>
                                    <option value="0" selected="selected">{{ text_disabled }}</option>
                                {% endif %}
                            </select>
                        </div>
                    </div>

                    <!-- Input PikPay key -->
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-pikpay-key"><span data-toggle="tooltip" title="{{ help_pikpay_key }}">{{ entry_profile_id }}</span></label>
                        <div class="col-sm-10">
                            <input type="text" name="payment_pikpay_key" value="{{ payment_pikpay_key }}" placeholder="{{ payment_pikpay_palceholder_key }}" id="input-pikpay-key" class="form-control" />
                            {% if error_payment_pikpay_key %}
                                <div class="text-danger">{{ error_payment_pikpay_key }}</div>
                            {% endif %}
                        </div>
                    </div>

                    <!-- Input PikPay Secret Key -->
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-secret-key"><span data-toggle="tooltip" title="{{ help_pikpay_secret_key }}">{{ entry_secret_key }}</span></label>
                        <div class="col-sm-10">
                            <input type="text" name="payment_pikpay_secret_key" value="{{ payment_pikpay_secret_key }}" placeholder="{{ payment_pikpay_palceholder_secret_key }}" id="input-secret-key" class="form-control" />
                            {% if error_secret_key %}
                                <div class="text-danger">{{ error_secret_key }}</div>
                            {% endif %}
                        </div>
                    </div>

                    <!-- Input payment processor -->
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-status">{{ entry_payment_processor }}</label>
                        <div class="col-sm-10">
                            <select name="payment_pikpay_processor" id="input-pikpay-processor" class="form-control">
                                {% if payment_pikpay_processor == 'webteh' %}
                                    <option value="webteh" selected="selected">{{ payment_pikpay_processor_hr }}</option>
                                    <option value="pikpay">{{ payment_pikpay_processor_ba }}</option>
                                {% else %}
                                    <option value="webteh">{{ payment_pikpay_processor_hr }}</option>
                                    <option value="pikpay" selected="selected">{{ payment_pikpay_processor_ba }}</option>
                                {% endif %}
                            </select>
                        </div>
                    </div>

                    <!-- Input payment autorizacija  -->
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-transaction-type">{{ entry_payment_transaction_type }}</label>
                        <div class="col-sm-10">
                            <select name="payment_pikpay_transaction_type" id="input-pikpay-transaction-type" class="form-control">
                                {% if payment_pikpay_transaction_type == 'purchase' %}
                                    <option value="purchase" selected="selected">{{ payment_pikpay_transaction_purchase }}</option>
                                    <option value="authorize">{{ payment_pikpay_transaction_authorize }}</option>
                                {% else %}
                                    <option value="purchase">{{ payment_pikpay_transaction_purchase }}</option>
                                    <option value="authorize" selected="selected">{{ payment_pikpay_transaction_authorize }}</option>
                                {% endif %}
                            </select>
                        </div>
                    </div>

                    <!-- Input odabir jezika -->
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-language">{{ entry_language }}</label>
                        <div class="col-sm-10">
                            <select name="payment_pikpay_language" id="input-pikpay-language" class="form-control">
                                {% if payment_pikpay_language == 'en' %}
                                    <option value="en" selected="selected">{{ entry_language_en }}</option>
                                    <option value="hr">{{ entry_language_hr }}</option>
                                    <option value="ba">{{ entry_language_ba }}</option>
                                {% elseif payment_pikpay_language == 'hr' %}
                                    <option value="en">{{ entry_language_en }}</option>
                                    <option value="hr" selected="selected">{{ entry_language_hr }}</option>
                                    <option value="ba">{{ entry_language_ba }}</option>
                                {% else %}
                                    <option value="en">{{ entry_language_en }}</option>
                                    <option value="hr">{{ entry_language_hr }}</option>
                                    <option value="ba" selected="selected">{{ entry_language_ba }}</option>
                                {% endif %}
                            </select>
                        </div>
                    </div>

                    <!-- Success i Fail linkovi  -->
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-language"></label>
                        <div class="col-sm-10">
                           <h4><b>"Success URL" and "Fail URL" copy to PikPay API settings.</b></h4>
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-language">{{ entry_success_url }}</label>
                        <div class="col-sm-10">
                            <input type="text" name="payment_pikpay_success_url" value="{{ success_url }}" id="input-success-url" class="form-control" readonly/>
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-language">{{ entry_fail_url }}</label>
                        <div class="col-sm-10">
                            <input type="reado" name="payment_pikpay_success_url" value="{{ fail_url }}" id="input-success-url" class="form-control" readonly/>
                        </div>
                    </div>

                </form>

            </div>
        </div>
    </div>
</div>
{{ footer }}